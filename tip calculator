#If the bill was $150.00, split between 5 people, with 12% tip. 

#Each person should pay (150.00 / 5) * 1.12 = 33.6
#Format the result to 2 decimal places = 33.60

#Tip: There are 2 ways to round a number. You might have to do some Googling to solve this.💪

#Write your code below this line 👇

print("welcome to tips calculator")

total_bill_is = input("What was the total bill? $")
tip_percentage = input("What percentage tip would you like to give? 10, 12, or 15? ")
people_to_split = input("How many people to split the bill? ")

total_bill_is_float = float(total_bill_is)
tip_percentage_float = float(tip_percentage)
people_to_split_int = int(people_to_split)

tip_percentage_float_result = (tip_percentage_float / 100) + 1
total_bill_is_float_result = total_bill_is_float * tip_percentage_float_result
total_bill_is_float_result_rounded = round(total_bill_is_float_result, 2 )
total_bill_is_float_result_rounded = "{:.2f}".format(total_bill_is_float_result)

print(f"Each person should pay: ${total_bill_is_float_result_rounded}")
